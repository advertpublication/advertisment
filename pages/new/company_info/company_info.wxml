<view class="info">
  <view class="header">
    <swiper indicator-dots="{{true}}" current="0">
      <block wx:for="{{qualificationImag}}" data-item="{{item}}">
        <swiper-item>
          <image class="picture" bindtap="imgYu" data-src="{{item}}" src="{{item}}"></image>
        </swiper-item>
      </block>
    </swiper>
  </view>
</view>